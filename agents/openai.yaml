version: 1
interface:
  display_name: Conductor
  short_description: Take over and operate a Gemini-first Conductor workspace
  default_prompt: Use the repo-local conductor workflow and bundled scripts to inspect, migrate when required, and operate a Gemini-compatible track-based Conductor workspace without reshaping existing official artifacts, while preserving branch and workflow checkpoints required by the repository policy.
